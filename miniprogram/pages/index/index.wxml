<view class="contain">
  <view class="title">
    小程序学习实例
  </view>
  <view class="content">
    <view class="content__item" wx:for="{{ navigateArray }}" wx:for-item="item" wx:key="index" bind:tap="onTap" data-url="{{ item.url }}">
      {{ item.title }}
    </view>
  </view>

</view>